Array: Un array es un objeto en el cual se pueden almacenar multiples elementos, por ejemplo la mochila que llevas al colegio, es un array porque es un objeto el cual contiene mas objetos, y no esta limitado a un tipo de objeto, sino que se pueden almacenar distintos tipos de elementos como cuadernos, libros, calculadora e inclusive se puede tener otro 'arreglo' dentro, siguiendo el ejemplo podemos tener una bolsita dentro de la mochila que nos ayude a guardar mas elementos, quiza mas pequeños como colores, lapices, plumas, etc, y esta bolsita de colores es un arreglo dentro de otro  (mochila).
